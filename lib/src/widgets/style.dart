import 'package:flutter/widgets.dart';


class StyleDarkMode {
  static const mainColor = Color.fromRGBO(245, 78, 0, 1);
  static const backgroundColor = Color.fromRGBO(0, 0, 0, 1);
  static const itemBackgroundColor = Color.fromRGBO(60, 60, 60, 1);
  static const textFieldBackgroundColor = Color.fromRGBO(40, 40, 40, 1);
  static const textColor = Color.fromRGBO(220, 220, 220, 1);
  // static const textImportantColor = Color.fromRGBO(220, 220, 220, 1);
  static const textGreyColor = Color.fromRGBO(160, 160, 160, 1);
  static const titleColor = Color.fromRGBO(240, 240, 240, 1);
}

class StyleTextDefault {
  static const margin = EdgeInsets.all(0);
  static const padding = EdgeInsets.all(0);
  static const color = Color.fromRGBO(0, 0, 0, 1);
  static const double fontSize = 20;
}

class StyleTextImportant {
  static const margin = EdgeInsets.all(10);
  static const padding = EdgeInsets.all(10);
  static const color = Color.fromRGBO(0, 0, 0, 1);
  static const double fontSize = 24;
}

class StyleTextSelected {
  // static const margin = EdgeInsets.all(0);
  // static const padding = EdgeInsets.all(0);
  static const color = Color.fromRGBO(245, 78, 0, 1);
  static const double fontSize = 20;
}

class StyleTextField {
  // static const margin = EdgeInsets.all(0);
  static const double fontSize = 16;
  static const double cursorHeight = 22;
  static const double borderRadius = 23;  
  static const double searchTextFieldHeight = 34;

}

class StyleUiTitle {
  static const margin = EdgeInsets.all(20);
  static const padding = EdgeInsets.all(20);
  static const color = Color.fromRGBO(0, 0, 0, 1);
  static const double fontSize = 32;
}